# Unused ディレクトリ

開発中にテストやトレーディングカード生成のプレビューなどで使用していたファイルを、確認用にここへまとめています。**削除は行わず、必要に応じて参照できるように保管しています。**

## 内容一覧

| パス | 説明 |
|------|------|
| `sample/` | 参考用サンプル。旧コンポーネント（GachaMachine.vue, MinecraftViewer.vue, SsrCard.vue）、Student.ts、stitch 関連（Carddass.html / .png, code.html, screen.png） |
| `inspect_genai.py` | GenAI 検証用のルート置き Python スクリプト |
| `building-2.glb` | ルートにあった 3D モデル（本番は `public/building-2.glb` を使用） |
| `tcg_generator_preview_output/` | トレーディングカード生成スクリプトのプレビュー出力画像（art_only.png, test_preview.png, ur_full.png, ur_preview.png） |

## 注意

- 本番ビルドやアプリの動作には使用していません。
- 必要になった場合はこのディレクトリ内を参照してください。
